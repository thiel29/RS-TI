/*8. Crie uma função que remove um elemento de uma posição específica de um
array. O índice do elemento a ser removido deve ser passado como
argumento.*/

let frutas = ["morango", "uva", "pera", "abacaxi"];
const manipulacao = (array, posicao);
